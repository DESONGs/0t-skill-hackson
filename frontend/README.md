# Frontend Console

这里放 `0t-skill_enterprise` 的前端静态文件。

当前实现是：

- 原生 HTML / CSS / JavaScript
- 由 `src/ot_skill_enterprise/frontend_server.py` 提供静态页面和 API
- 默认不依赖 npm 或额外前端构建链

用途：

- 浏览 runtime、sessions、active runs 状态
- 作为只读 runtime dashboard 的第一版信息架构基线
