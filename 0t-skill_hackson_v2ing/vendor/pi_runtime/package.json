{
  "name": "ot-vendored-pi-runtime",
  "private": true,
  "type": "module",
  "workspaces": [
    "upstream/agent",
    "upstream/ai",
    "upstream/tui",
    "upstream/coding_agent"
  ],
  "devDependencies": {
    "esbuild": "^0.25.11",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "build:ot-runtime": "node --input-type=module --eval \"import { build } from 'esbuild'; await build({ entryPoints: ['upstream/coding_agent/src/ot_runtime_entry.ts'], bundle: true, format: 'esm', platform: 'node', target: ['node20.6'], outfile: 'dist/pi-runtime.mjs', banner: { js: '#!/usr/bin/env node' }, sourcemap: false });\"",
    "verify:ot-runtime": "node --check dist/pi-runtime.mjs"
  }
}
