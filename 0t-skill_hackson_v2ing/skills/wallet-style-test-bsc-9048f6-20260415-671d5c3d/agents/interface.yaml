interface:
  display_name: wallet-style-test-bsc-9048f6-20260415
  short_description: High-frequency BSC scalper specializing in micro-cap memecoins
    via WBNB quote pairs. Exhibits same-minute burst execution, 21-second average
    holds, pyramid averaging into positions, and diamond-hands drawdown tolerance
    up to -61%. Zero stablecoin allocation with near-complete capital deployment per
    trade cycle.
  default_prompt: High-frequency BSC scalper specializing in micro-cap memecoins via
    WBNB quote pairs. Exhibits same-minute burst execution, 21-second average holds,
    pyramid averaging into positions, and diamond-hands drawdown tolerance up to -61%.
    Zero stablecoin allocation with near-complete capital deployment per trade cycle.
compatibility:
  canonical_format: agent-skills
  adapter_targets:
  - generic
  activation:
    mode: manual
    paths: []
  execution:
    context: fork
    shell: bash
  trust:
    source_tier: local
    remote_inline_execution: forbid
    remote_metadata_policy: explicit-providers-only
  degradation:
    generic: manual
